---
  - name: "Create VM"
    hosts: localhost
    gather_facts: no
    vars:
      #ansible_python_interpreter: /usr/bin/python2.7
      ansible_python_interpreter: /usr/bin/python
    roles:
      - Esx  
